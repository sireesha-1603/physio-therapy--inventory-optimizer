import { useState } from 'react'
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { useData } from '../context/DataContext'
export function InventoryOutcomesPage({addToast}){const {outcomes,feedback,addOutcomeFeedback}=useData();const [message,setMessage]=useState('');const submit=async e=>{e.preventDefault();try{await addOutcomeFeedback(message);setMessage('');addToast('Feedback saved','Outcome feedback is now persisted.','success')}catch{addToast('Unable to save feedback','Please try again.','error')}};return <div><section className="metrics"><article className="metric"><div><span className="metric-label">Forecast Accuracy</span><strong>{outcomes.accuracy}%</strong></div></article><article className="metric"><div><span className="metric-label">Approved Recommendations</span><strong>{outcomes.adoption}</strong></div></article></section><div className="panel"><h2>Actual vs predicted outcomes</h2><ResponsiveContainer width="100%" height={250}><BarChart data={outcomes.rows}><CartesianGrid vertical={false}/><XAxis dataKey="category"/><YAxis/><Tooltip/><Bar dataKey="predicted" fill="#c7d2fe"/><Bar dataKey="actual" fill="#4f46e5"/></BarChart></ResponsiveContainer></div><div className="panel"><h2>Outcome feedback</h2><form onSubmit={submit} style={{display:'flex',gap:10}}><input required className="form-input" value={message} onChange={e=>setMessage(e.target.value)} placeholder="Record an actual outcome or model observation…"/><button className="btn-primary">Save feedback</button></form>{feedback.map(record=><p key={record._id} style={{fontSize:12}}>{record.message} <small>— {new Date(record.createdAt).toLocaleString()}</small></p>)}</div></div>}
