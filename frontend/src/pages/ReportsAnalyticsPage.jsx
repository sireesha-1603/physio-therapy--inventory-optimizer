import { useData } from '../context/DataContext'
export function ReportsAnalyticsPage({addToast,onOpenExport}){const {items,reports,generateReport}=useData();const create=async(format,range)=>{try{await generateReport(format,range);addToast('Report generated',`${format.toUpperCase()} report saved in history.`,'success')}catch{addToast('Report generation failed','Please try again.','error')}};const value=items.reduce((sum,item)=>sum+item.stock*item.unitCost,0);return <div><div className="filter-bar" style={{justifyContent:'space-between'}}><strong>Analytics & report exporter</strong><button className="btn-primary" onClick={()=>onOpenExport((format,range)=>create(format,range))}>Generate report</button></div><section className="metrics"><article className="metric"><div><span className="metric-label">Inventory value</span><strong>₹{value.toLocaleString()}</strong></div></article><article className="metric"><div><span className="metric-label">Active SKUs</span><strong>{items.length}</strong></div></article></section><div className="panel"><h2>Generated reports</h2><div className="table-wrap"><table><thead><tr><th>NAME</th><th>FORMAT</th><th>RANGE</th><th>DATE</th><th>DOWNLOAD</th></tr></thead><tbody>{reports.length?reports.map(r=><tr key={r._id}><td>{r.name}</td><td>{r.format.toUpperCase()}</td><td>{r.range}</td><td>{new Date(r.createdAt).toLocaleString()}</td><td><a className="btn-outline" href={`http://localhost:5000/api/v1/operations/reports/${r._id}/download`} target="_blank">Download</a></td></tr>):<tr><td colSpan="5">No reports generated.</td></tr>}</tbody></table></div></div></div>}
